services:
  - type: web
    name: saas-crm-fastapi-api
    env: docker
    plan: free
    autoDeploy: true
    dockerfilePath: docker/api/Dockerfile
    preDeployCommand: alembic upgrade head
    healthCheckPath: /api/v1/health
    envVars:
      - key: ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: saas-crm-fastapi-db
          property: connectionString
      - key: LLM_ENABLED
        value: "false"

databases:
  - name: saas-crm-fastapi-db
    plan: free
    databaseName: app
    user: postgres
